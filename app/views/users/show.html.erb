<h1>
    <%= @user.first_name %> <%= @user.last_name %>
    
</h1>
<% if @user == current_user %>
    <%= link_to "Editer mon profil", edit_user_path(current_user), class: "btn btn-outline-primary" %>
<% end %>
